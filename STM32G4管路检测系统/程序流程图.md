# STM32G4 压力流量监控系统 —— 程序流程图

> 每张图一个流程图。开头 `{调用哪个函数?}` 分支，每个函数一支，汇合到 Z([返回]) 出口。
> 中文描述，函数名/变量名/常量保持英文。判断框用 是/否。循环回到最小编号入口。

## draw.io 渲染避坑规则（必读）

draw.io 内置的是 **Mermaid v10.5.0 精简版**，比 mermaid.live 严格得多。**测试发现 8 个流程图里 adc_sensor 一个画布空白、其他 7 个正常画出来**，根因只有一个：

**节点文本里出现 `*` 这个字符且未加双引号，draw.io 解析器直接当 token 报错，画布空白。**

排查对照表（按炸的概率排序）：

| 排名 | 雷区 | 炸的方式 | 修复 |
|---|---|---|---|
| 1 | `[...]` 文本里出现裸 `*` | **100% 报语法错**（实测元凶） | 节点加双引号：`A["Vin = adc * 3.3 / 4096"]` |
| 2 | `%%{init: {...}}%%` 头 | 解析器跳过此行，画布仍能出 | 可删；想保留也没事（推荐删） |
| 3 | `([椭圆])` 语法 | 部分 10.5.0 解析失败 | 改成 `("圆角矩形")` |
| 4 | `{判断框文本}` 含 `?` 或中文 | 不报错但中文可能乱 | 改成 `{"判断框文本"}`（去问号 + 加引号） |
| 5 | `linkStyle default curve:step` | 被忽略，不报错 | 删 |
| 6 | `classDef` / `class` | 被忽略，不报错 | 删 |
| 7 | ID 含空格（如 `A Node`） | 报 panic | 改 `ANode` |
| 8 | 中文节点未加引号 | UTF-8 tokenization 失败 | 全部加双引号：`A["中文"]` |

**最稳的写法模板：**

```mermaid
flowchart TD
    A("入口") --> B{"判断"}
    B -->|是| C["步骤 1"]
    B -->|否| D["步骤 2"]
    C --> Z("返回")
    D --> Z
```

> 规则：**所有含中文、空格、`*`/`?`/特殊符号的节点，都用双引号包起来**。

---

## 模块列表

| # | 模块 | 包含函数 | 职责 |
|---|---|---|---|
| 1 | **app** | `app_init()` / `app_run()` | 初始化 LCD/ADC/PWM/Flow + 主循环调度 |
| 2 | **key** | `key_scan()` / `key_handle()` | 按键状态机 + 长短按事件分发 |
| 3 | **lcd_ui** | `lcd_ui_init()` / `lcd_Show_Page1~3()` / `Lcd_Refresh()` | 三页 LCD 显示与 100ms 周期刷新 |
| 4 | **adc_sensor** | `adc_sensor_init()` / `adc_get_voltage()` / `adc_get_pressure()` | ADC 采样 + 电压 → 压力映射 + Voffset |
| 5 | **pwm** | `pwm_init()` / `pwm_apply_duty()` / `pwm_ramp_tick()` | PA1 TIM2_CH2 + PA7 TIM17_CH1 同步 + 1%/s 斜坡 + MAN/AUTO 算法 |
| 6 | **flow** | `flow_init()` / `flow_ic_capture()` / `flow_accumulate_q()` | TIM16 测频 + 瞬时流量 + Q 累计 |
| 7 | **led** | `led_show()` / `led_tick()` | 74HC595 驱动 + LD1~LD4 状态判定 + 2s 去抖 |
| 8 | **timer_task** | `timer_task_dispatch()` | TIM3 中断里 50/100/1000ms 三档周期分派 |

> 注：`settings` 模块仅含全局变量声明（无函数），不画流程图。

---

## 0. 主程序流程图（main.c）

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([上电复位]) --> B[main 入口]
    B --> C[HAL_Init + SystemClock_Config]
    C --> D[MX_GPIO / ADC2 / TIM2 / TIM3 / TIM16 / TIM17 初始化]
    D --> E[app_init]
    E --> F[主循环 while 1]
    F --> G[app_run: key_handle + Lcd_Refresh]
    G --> F

    H[TIM3 1ms 周期中断] -.硬件触发.-> I[timer_task_dispatch]
    J[TIM16 上升沿捕获中断] -.硬件触发.-> K[flow_ic_capture]
```

---

## 1. app 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([app 模块入口]) --> B{调用哪个函数?}
    B -->|app_init| C[led_show 0x00 关闭所有 LED]
    C --> D[lcd_ui_init LCD 黑底白字]
    D --> E[adc_sensor_init ADC2 内部偏移校准]
    E --> F[pwm_init 启动 TIM2_CH2 占空比 5%]
    F --> G[flow_init 启动 TIM16 CH1 捕获中断]
    G --> H[HAL_TIM_Base_Start_IT 启动 TIM3 1ms 中断]
    H --> Z([返回])
    B -->|app_run| I[key_handle 4 按键事件分发]
    I --> J[Lcd_Refresh 整页重绘 + 监控页 100ms 数据刷新]
    J --> Z
```

---

## 2. key 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    linkStyle default curve:step

    A([key 模块入口]) --> B{调用哪个函数?}

    subgraph key_scan [按键扫描状态机]
        B -->|key_scan| C[读 4 个 GPIO 写入 key 0..3 的 key_now]
        C --> D[遍历 i = 0..3]
        D --> E{key i 的 key_judge 当前状态}
        E -->|0 空闲| F{key_now 是否为 0?}
        F -->|是| G[记 key_press_tick, key_long_fired = 0, 切到 1]
        F -->|否| X
        G --> X
        E -->|1 确认| H{key_now 是否仍为 0?}
        H -->|是| I[切到 2]
        H -->|否| J[切到 0 误触]
        I --> X
        J --> X
        E -->|2 持续| K{key_now 是否为 1 松手?}
        K -->|是| L{key_long_fired 是否为 0?}
        L -->|是| M[key_duan_flag = 1 短按事件]
        L -->|否| N[忽略 本次按为长按]
        M --> O[切到 0]
        N --> O
        O --> X
        K -->|否| P{到 2s 整且未触发长按?}
        P -->|是| Q[key_long_flag = 1, key_long_fired = 1 长按]
        P -->|否| X
        Q --> X
        X --> Y{遍历完?}
        Y -->|否| E
        Y -->|是| Z([返回])
    end

    subgraph key_handle [按键事件处理]
        B -->|key_handle| K1[B1 短按切页 + 离开 OUTP 触发 LD4]
        K1 --> K2[B2 短按 模式 MAN 与 AUTO 与选项切换]
        K2 --> K3[B3 与 B4 短按 加减 OUTP 预设与 PARA 阈值]
        K3 --> K4[B3 长按 监控页 Voffset = Vin_Voltage 零点校准]
        K4 --> K5[B4 长按 监控页 Q_value = 0, Q_acc = 0 清累计]
        K5 --> Z
    end
```

---

## 3. lcd_ui 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([lcd_ui 模块入口]) --> B{调用哪个函数?}
    B -->|lcd_ui_init| C[LCD_Init + LCD_Clear Black + 设色黑底白字]
    C --> Z([返回])
    B -->|lcd_Show_Page1| D[清屏 + 写标题 MAIN + M=MAN/AUTO]
    D --> E[写 P=xx.x BAR F=xx.x L/M Q=xxL D=xx% V=xx.x V]
    E --> Z
    B -->|lcd_Show_Page2| F[清屏 + 写标题 OUTP + TarD/TarP 两行]
    F --> Z
    B -->|lcd_Show_Page3| G[清屏 + 写标题 PARA + FH/FL/PL/DH 四行]
    G --> Z
    B -->|Lcd_Refresh| H{是否切页 Lcd_Refresh_Flag = 1?}
    H -->|是| I[清屏 + 按 Lcd_Now_Page 调对应 Show 函数整页重绘]
    I --> Z
    H -->|否| J{在监控页且距上次 100ms?}
    J -->|是| K[刷 Line3~Line8 数据行 标题不刷避闪]
    J -->|否| Z
    K --> Z
```

---

## 4. adc_sensor 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([adc_sensor 模块入口]) --> B{调用哪个函数?}
    B -->|adc_sensor_init| C[HAL_ADCEx_Calibration_Start ADC2 单端校准]
    C --> Z([返回])
    B -->|adc_get_voltage| D[HAL_ADC_Start 触发 ADC2 转换]
    D --> E[HAL_ADC_PollForConversion 等待 10ms 超时保护]
    E --> F{转换成功?}
    F -->|是| G[adc_value2 = HAL_ADC_GetValue]
    F -->|否| H[跳过 保留上次 adc_value2]
    G --> I[Vin_Voltage = adc_value2 * 3.3 / 4096]
    H --> I
    I --> Z
    B -->|adc_get_pressure| J[v = adc_get_voltage 取电压]
    J --> K{v 是否小于等于 Voffset?}
    K -->|是| L[P_value = 0]
    K -->|否| M[range = 3.3 - Voffset, 最小 0.01 防除零]
    M --> N[P_value = (v - Voffset) * 10 / range, 钳到 10.0]
    L --> Z
    N --> Z
```

---

## 5. pwm 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([pwm 模块入口]) --> B{调用哪个函数?}
    B -->|pwm_init| C[D_actual 设为 5]
    C --> D[启动 TIM2_CH2 PA1 10kHz CCR=5]
    D --> DA[启动 TIM17_CH1 PA7 50Hz CCR=1500 中位]
    DA --> Z([返回])
    B -->|pwm_apply_duty| F[写 TIM2 CCR = D_actual 比例阀 5-95]
    F --> FA[CCR_pa7 = 500 + D_actual-5 × 200/9 舵机脉冲 500-2500us]
    FA --> FB[写 TIM17 CCR = CCR_pa7]
    FB --> Z
    B -->|pwm_ramp_tick| G{当前模式 B2_flag1?}
    G -->|MAN| H{D_actual 与 TarD_value 大小?}
    H -->|小| I[D_actual++]
    H -->|大| J[D_actual−−]
    H -->|相等| K[D 不变 tar_changed = 0 灭 LD4]
    I --> L[D 越界钳到 5~95]
    J --> L
    K --> L
    G -->|AUTO| M{P 与 TarP 关系?}
    M -->|P 小于 TarP−0.5| N[D_actual 小于 95 则 D_actual++]
    M -->|P 大于 TarP+0.5| O[D_actual 大于 5 则 D_actual−−]
    M -->|P 在死区| P[tar_changed = 0 灭 LD4]
    N --> Q[pwm_apply_duty 写两个 CCR PA1+PA7 同步]
    O --> Q
    P --> Q
    L --> Q
    Q --> Z
```

---

## 6. flow 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([flow 模块入口]) --> B{调用哪个函数?}
    B -->|flow_init| C[HAL_TIM_IC_Start_IT TIM16 CH1 上升沿捕获]
    C --> Z([返回])
    B -->|flow_ic_capture| D[读捕获值 fre_count1 清计数器 CNT=0]
    D --> E{fre_count1 大于 0?}
    E -->|否| Z
    E -->|是| F[fre1 = 1000000 / fre_count1 1MHz 计数时钟]
    F --> G[F_value = fre1 / 200]
    G --> H{fre1 大于 8000?}
    H -->|是| I[fre1 钳 8001 F_value 钳 40 触发 LD1 异常]
    H -->|否| J{fre1 小于 800?}
    J -->|是| K[F_value = 0 视为无流量]
    J -->|否| Z
    I --> Z
    K --> Z
    B -->|flow_accumulate_q| L[Q_acc += F_value × 0.1 / 60 100ms 折算升]
    L --> M{Q_acc 大于等于 1.0?}
    M -->|否| Z
    M -->|是| N[Q_value += Q_acc 整数部分]
    N --> O[Q_acc 减去整数部分 保留小数]
    O --> P{Q_value 大于 999999999?}
    P -->|是| Q[Q_value 钳到 999999999 防溢出]
    P -->|否| Z
    Q --> Z
```

---

## 7. led 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([led 模块入口]) --> B{调用哪个函数?}
    B -->|led_show| C[PC8~PC15 全部置 1 全灭]
    C --> D[PD2 拉高再拉低 锁存全灭状态]
    D --> E[GPIOC 写入 ucled 左移 8 低电平点亮对应位]
    E --> F[PD2 拉高再拉低 锁存最终状态]
    F --> Z([返回])
    B -->|led_tick| G[清空 new_led = 0x00]
    G --> H{fre1 大于 8000?}
    H -->|是| I[new_led 置位 LD1_BIT 传感器异常]
    H -->|否| J{F 小于 FL 且 D 大于 DH?}
    I --> J
    J -->|是| K[led2_t 累加 LED_SCAN_MS]
    K --> L{led2_t 达到 2000ms?}
    L -->|是| M[new_led 置位 LD2_BIT 堵塞]
    L -->|否| N{F 大于 FH 且 P 小于 PL?}
    J -->|否| O[led2_t = 0 清去抖计时]
    M --> N
    O --> N
    N -->|是| P[led3_t 累加 LED_SCAN_MS]
    P --> Q{led3_t 达到 2000ms?}
    Q -->|是| R[new_led 置位 LD3_BIT 泄漏]
    Q -->|否| S{是否 tar_changed?}
    N -->|否| T[led3_t = 0 清去抖计时]
    R --> S
    T --> S
    S -->|是| U[new_led 置位 LD4_BIT 动态调节]
    S -->|否| V{new_led 与 led_state 不同?}
    U --> V
    V -->|是| W[led_state = new_led 调 led_show 刷 74HC595]
    V -->|否| Z
    W --> Z
```

---

## 8. timer_task 模块

```mermaid
%%{init: {"flowchart": {"curve": "step"}}}%%
flowchart TD
    A([TIM3 1ms 中断入口]) --> B{调用哪个任务?}
    B -->|每次中断| C[key_scan 4 按键状态机]
    C --> Z([退出中断返回])
    B -->|距上次 50ms| D[led_tick 判定 LD1~LD4 写 74HC595]
    D --> Z
    B -->|距上次 100ms| E[adc_get_pressure 读 ADC 算 P_value]
    E --> F[flow_accumulate_q F 累到 Q_acc 满 1L 进 Q_value]
    F --> Z
    B -->|距上次 1000ms| G[pwm_ramp_tick MAN 追 TarD 或 AUTO 按 P−TarP 调 D]
    G --> Z
```
